## JVM（五大块儿）
- 方法区 Method Area 
- 堆 Heap          
- 虚拟机栈 Stack 
- 本地方法栈     
- 程序计数器 PC  

### 堆和栈
```
public void test() {
    int a = 1;
    User u = new User();
}

栈里：
a
u（引用）

堆里：
User对象

栈：存放基本类型和对象引用，生命周期随方法调用结束而结束
堆：存放对象实例，生命周期由垃圾回收器管理
```
- 栈方法结束，变量直接销毁
- 堆需要GC，因为可能有多个地方引用了这个对象